<div class="bucket">
  <%= view.branch_name %>
  <span class="hidden">
    <% view.nwo? %>
      <a href="<%= view.compare_url %>" target="_blank"><%= view.short_sha %></a>
    <% else %>
      <%= view.short_sha %>
    <% end %>
  </span>
</div>
